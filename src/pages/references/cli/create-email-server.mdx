import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import NextPage from "@/components/Common/nextpage";
import Asciinema from "@/components/Common/asciinema";
import Table from "@/components/Common/table";

import Head from "next/head";

<Layout>
<Head>
<title>مستندات ساخت و ایجاد ایمیل‌سرور با Liara CLI - لیارا</title>
<meta property="og:title" content="مستندات خدمات رایانش ابری لیارا" />
<meta property="og:description" content="مستندات مربوط به نحوه ساخت و ایجاد ایمیل‌سرور در رابط کاربری خط فرمان لیارا"  />
<meta property="og:image" content="https://files.liara.ir/liara/logos/liara-poster.jpg" />
</Head>
# ایجاد ایمیل‌سرور با Liara CLI
<hr className="mb-2" />

برای 
ایجاد ایمیل‌سرور با Liara CLI،
می‌توانید از دستور زیر، استفاده کنید: 

<div className="h-2" />
<div dir="ltr">
<Highlight className='bash'>
{`liara mail create`}
</Highlight>
</div>
<div className="h-2" />
{/* <Asciinema id="create-db-using-cli" /> */}


<Section id="command-parameters" title="پارامترهای دستور" />
دستور <Important>liara mail create</Important>، پارامترهای زیر را، می‌پذیرد:

<div className="h-2" />
<div dir="ltr">
<Highlight className='bash'>
{`liara mail create [-h] [--debug] [--api-token <value>] [--account <value>] [--domain <value>]
[--plan <value>] [--mode <value>] [--team-id <value>]`}
</Highlight>
</div>

<Table headers={[
  "پارامتر",
  "توضیحات",
  "مثال",
]} data={[
  [
    <Important>h- یا help--</Important>,
    "نمایش راهنما",
    <Important>liara mail create -h</Important>,
  ],
  [
    <Important>debug--</Important>,
    "برای نمایش لاگ‌های دیباگ",
    <Important>liara mail create --debug</Important>,
  ],
   [
    <Important>team-id--</Important>,
    <p>انتخاب تیم با استفاده از <a href="/references/api/about/#team-id" className="text-[#2196f3]">شناسه آن</a></p>,
    <Important>liara mail create --team-id=your-team-id</Important>,
  ],
  [
    <Important>api-token--</Important>,
    <div dir="rtl">
    <p> برای اجرای آنی عملیات ساخت ایمیل‌سرور به کمک <a href="/references/api/about" className="text-[#2196f3] ">api token</a> (مناسب ci/cd)</p>
    </div>,
    <Important>****=liara mail create --api-token</Important>,
  ],
  [
    <Important>account--</Important>,
    "برای تغییر موقتی حساب کاربری ",
    <Important>liara mail create --account jane_doe</Important>,
  ],
  [
    <Important>domain--</Important>,
    "برای درج دامنه ایمیل‌سروری که قصد ایجاد آن را دارید",
    <Important>liara mail create --domain=example.com</Important>,
  ],
  [
    <Important>mode--</Important>,
    <div dir="rtl">
    <p>برای تعیین حالت ایمیل‌سرور (LIVE & DEV)</p>
    </div>,
    <Important>liara mail create --mode=DEV</Important>,
  ],
  [
    <Important>plan--</Important>,
    <div dir="rtl">
    <p>برای انتخاب پلن ایمیل‌سرور</p>
    </div>,
    <Important>liara mail create --plan=free</Important>,
  ],

]} />
<div className="h-3" />

</Layout>