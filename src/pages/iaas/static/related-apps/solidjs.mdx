import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import NextPage from "@/components/Common/nextpage";

import Head from "next/head";

<Layout>
<Head>
<title>مستندات استقرار برنامه‌های SolidJS - لیارا</title>
<meta property="og:title" content="مستندات خدمات رایانش ابری لیارا" />
<meta property="og:description" content="مستندات مربوط به نحوه استقرار برنامه‌های SolidJS در پلتفرم Static در لیارا"  />
<meta property="og:image" content="https://files.liara.ir/liara/logos/liara-poster.jpg" />
</Head>
# استقرار برنامه‌های SolidJS در لیارا
<hr className="mb-2" />

<a href="https://docs.solidjs.com/" className="text-[#2196f3] ">SolidJS</a>
یک فریمورک جاوااسکریپت برای ساخت رابط‌های کاربری (UI) است که به طور خاص بر روی سرعت و کارایی متمرکز است. این فریمورک از الگوی واکنش‌گرایی (reactivity) استفاده می‌کند تا با استفاده از تغییرات دقیق و کارآمد در DOM، به سرعت بسیار بالایی دست یابد. برخلاف برخی فریمورک‌های دیگر مثل React، SolidJS از ابتدا طراحی شده تا تنها کامپوننت‌های تغییر یافته را دوباره رندر کند، که این ویژگی باعث می‌شود عملکرد بهینه‌تری داشته باشد.
<div className="h-2" />

شما می‌توانید برنامه‌های SolidJS خود را با <a href="../../how-tos/create-app" className="text-[#2196f3] ">ایجاد برنامه‌های Static</a> در لیارا، مستقر کنید.
برای استقرار برنامه‌های SolidJS 
کافیست تا در مسیر اصلی پروژه در لوکال، دستور زیر را اجرا کنید:

<div className="h-2" />
<div dir='ltr'>
    <Highlight className="bash">
        {`npm run build`}
    </Highlight>
</div>
<div className="h-2" />

بعد از انجام این کار، یک دایرکتوری به نام <Important>dist</Important> برای شما ایجاد می‌شود. 

<Section id="deploy" title="استقرار برنامه" />
<Tabs 
    tabs={["Liara Console", "Liara CLI"]} 
    content={[
    <>
    <p>
    در نهایت کافیست تا دایرکتوری <Important>dist</Important> را درون یک فایل zip قرار دهید و برنامه خود را با کنسول در لیارا آپلود کنید تا برنامه با موفقیت در لیارا مستقر شود. 
    </p>
    </>,
 
    <>
    <p>
    در نهایت کافیست تا وارد دایرکتوری <Important>dist</Important> شوید و دستور زیر را اجرا کنید تا برنامه شما در لیارا مستقر شود:
    </p>
    <div className="h-2" />
    <div dir='ltr'>
        <Highlight className="bash">
            {`liara deploy --platform=static`}
        </Highlight>
    </div>
    <div className="h-2" />
    </>,

]} 
/>

<Alert variant="success">
<p>
یک پروژه SolidJS آماده به استقرار در <a href="https://github.com/liara-cloud/solidjs-getting-started" className="text-[#2196f3] ">اینجا</a> وجود دارد که می‌توانید از آن، استفاده کنید.
</p>
</Alert>
</Layout>

