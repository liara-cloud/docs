import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import NextPage from "@/components/Common/nextpage";

import Head from "next/head";

<Layout>
<Head>
<title>مستندات تغییر نسخه NodeJS در NextJS - لیارا</title>
<meta property="og:title" content="مستندات خدمات رایانش ابری لیارا" />
<meta property="og:description" content="مستندات مربوط به تغییر نسخه NodeJS در پلتفرم NextJS در لیارا"  />
<meta property="og:image" content="https://files.liara.ir/liara/logos/liara-poster.jpg" />
</Head>
# نسخه NodeJS در پلتفرم NextJS
<hr className="mb-2" />

در حال حاضر در لیارا، نسخه‌های زیر از NodeJS در پلتفرم NextJS ارائه می‌شود:

<div className="h-4" />
<ul className="pr-4">
  {[
    {text: '20 (پیش‌فرض)',},
    {text: '22',},
  ].map((item, index) => (
    <li
      key={index}
      style={{
        textDecorationColor: "#9ca3af"
      }}
    >
        نسخه {item.text}
    </li>
  ))}
</ul>

<Section id="change-default-version" title="تغییر نسخه پیش‌فرض" />
<Tabs 
    tabs={["Liara Console", "Liara CLI"]} 
    content={[
    <>
    <p>پس از بارگذاری پروژه خود در لیارا، کافیست تا در مرحله انتخاب نسخه، نسخه مورد نظر برنامه‌تان را انتخاب کنید.</p>
    <img src="https://files.liara.ir/liara/docs/change-nodejs-version-of-nextjs.png" alt="how to choose nodejs version in liara console" />
    </>,

    <>
    <p>
        برای تغییر نسخه پیش‌فرض، کافیست تا یک فایل به نام <Important>liara.json</Important> در مسیر اصلی پروژه، ایجاد کنید و قطعه کد زیر را درون آن، قرار دهید. قطعه کد زیر برای تغییر نسخه NodeJS به 22 می‌باشد:
    </p>
    <div className="h-2" />
    <div dir='ltr'>
        <Highlight className="json">
            {`{
    "next": {
    "nodeVersion": "22"
  }
}`}
        </Highlight>
    </div>
    <div className="h-2" />
    </>,

]} 
/>

</Layout>