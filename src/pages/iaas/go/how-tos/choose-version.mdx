import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import NextPage from "@/components/Common/nextpage";

import Head from "next/head";

<Layout>
<Head>
<title>مستندات انتخاب نسخه go در go - لیارا</title>

<meta property="og:title" content="مستندات خدمات رایانش ابری لیارا" />
<meta property="og:description" content="مستندات انتخاب نسخه در پلتفرم go در لیارا"  />
<meta property="og:image" content="https://files.liara.ir/liara/logos/liara-poster.jpg" />
</Head>
# انتخاب نسخه go 
<hr className="mb-2" />

در حال حاضر در پلتفرم go لیارا، نسخه‌های زیر ارائه می‌شوند: 

<div className="h-2" />
<ul className="pr-4">
  {[
    // {text: '3.7 (بدون به‌روزرسانی)',},
    {text: '1.21 ',},
    {text: '1.22 (پیش‌فرض)',},
    {text: '1.23',},
  ].map((item, index) => (
    <li
      key={index}
      style={{
        textDecorationColor: "#9ca3af"
      }}
    >
        نسخه {item.text}
    </li>
  ))}
</ul>

<div className="h-2" />
<Alert variant="warning">
<p>
 قابلیت‌های جدید برای نسخه‌های بدون به‌روزرسانی، ارائه نمی‌شوند و برای رفع این مشکل، باید ورژن go برنامه خود را، ارتقاء دهید.
</p>
</Alert>

<Section id="change-default-version" title="تغییر نسخه پیش‌فرض" />
<Tabs 
    tabs={["Liara Console", "Liara CLI", "Github"]} 
    content={[
    <>
    <p>پس از بارگذاری پروژه خود در لیارا، کافیست تا در مرحله انتخاب نسخه، نسخه مورد نظر برنامه‌تان را انتخاب کنید.</p>
    <img src="https://files.liara.ir/liara/go/go-choose-version-using-console.png" alt="how to choose version in liara console" />
    </>,
    <>
    <p>
        برای تغییر نسخه پیش‌فرض، کافیست تا یک فایل به نام <Important>liara.json</Important> در مسیر اصلی پروژه، ایجاد کنید و قطعه کد زیر را درون آن، قرار دهید. قطعه کد زیر برای تغییر نسخه go به 1.23 می‌باشد:
    </p>
    <div className="h-2" />
    <div dir='ltr'>
        <Highlight className="json">
            {`{
  "go": {
    "version": "1.23"
  }
}`}
        </Highlight>
    </div>
    <div className="h-2" />
    </>,
    <>
    <p>
        برای تغییر نسخه پیش‌فرض، کافیست تا یک فایل به نام <Important>liara.json</Important> در مسیر اصلی پروژه، ایجاد کنید و قطعه کد زیر را درون آن، قرار دهید. قطعه کد زیر برای تغییر نسخه go به 1.23 می‌باشد:
    </p>
    <div className="h-2" />
    <div dir='ltr'>
        <Highlight className="json">
            {`{
  "go": {
    "version": "1.23"
  }
}`}
        </Highlight>
    </div>
    <div className="h-2" />
    </>,

]} 
/>


</Layout>