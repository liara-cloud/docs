import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import NextPage from "@/components/Common/nextpage";

import Head from "next/head";

<Layout>
<Head>
<title>مستندات تنظیم فوروارد در ایمیل سرور - لیارا</title>
<meta property="og:title" content="مستندات خدمات رایانش ابری لیارا" />
<meta property="og:description" content="مستندات تنظیم فوروارد در ایمیل‌سرور"  />
<meta property="og:image" content="https://files.liara.ir/liara/logos/liara-poster.jpg" />
</Head>
# تنظیم فوروارد
<hr className="mb-2" />

فوروارد کردن (Forward) در یک ایمیل به معنای ارسال مجدد یک ایمیل دریافت شده به یک یا چند گیرنده جدید است. وقتی یک ایمیل را فوروارد می‌کنید، محتوای اصلی ایمیل به همراه هرگونه پیوست آن به گیرنده جدید ارسال می‌شود. این قابلیت به شما امکان می‌دهد تا به راحتی اطلاعات یا پیام‌های دریافت شده را با دیگران به اشتراک بگذارید.
<div className="h-4" />

شما می‌توانید در لیارا، برای هر نشانی ایجاد شده در ایمیل‌سرورتان، یک فوروارد، تنظیم کنید.
کافیست تا وارد صفحه **فورواردها** شده و در بخش نشانی مدنظرتان، روی گزینه **ایجاد فوروارد** کلیک کنید. 
در ادامه، باید نشانی مقصد را وارد کرده و بر روی گزینه **افزودن نشانی** کلیک کنید تا فوروارد تنظیم شود:

<div className="h-4" />
<img src="https://files.liara.ir/liara/docs/set-forward-for-email.gif" alt="email server" />
<div className="h-4" />
پس از انجام کار فوق، تمامی ایمیل‌های ارسال شده به نشانی تنظیم شده، به ایمیل تعیین شده شما نیز، ارسال می‌شود.
{/* <ul className="pr-4">
  {[
    {text: '8',},
    {text: '10',},
    {text: '12',},
    {text: '14',},
    {text: '16',},
    {text: '18',},
    {text: '20 (پیش‌فرض)',},
    {text: '22',},
  ].map((item, index) => (
    <li
      key={index}
      style={{
        textDecorationColor: "#9ca3af"
      }}
    >
        نسخه {item.text}
    </li>
  ))}
</ul>

<Section id="change-default-version" title="تغییر نسخه پیش‌فرض" />
<Tabs 
    tabs={["Liara Console", "Liara CLI"]} 
    content={[
    <>
    <p>پس از بارگذاری پروژه خود در لیارا، کافیست تا در مرحله انتخاب نسخه، نسخه مورد نظر برنامه‌تان را انتخاب کنید.</p>
    <img src="https://files.liara.ir/liara/docs/choose-nodejs-version.png" alt="how to choose nodejs version in liara console" />
    </>,

    <>
    <p>
        برای تغییر نسخه پیش‌فرض، کافیست تا یک فایل به نام <Important>liara.json</Important> در مسیر اصلی پروژه، ایجاد کنید و قطعه کد زیر را درون آن، قرار دهید. قطعه کد زیر برای تغییر نسخه NodeJS به 18 می‌باشد:
    </p>
    <div className="h-2" />
    <div dir='ltr'>
        <Highlight className="json">
            {`{
    "node": {
    "version": "18"
  }
}`}
        </Highlight>
    </div>
    <div className="h-2" />
    </>,

]} 
/>*/}

</Layout>