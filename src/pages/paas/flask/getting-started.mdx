import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import PlatformIcon from "@/components/Common/icons";

import {
  GoContainer,
  GoDatabase,
  GoRocket,
  GoServer,
  GoMail,
  GoGlobe,
  GoArrowLeft,
} from "react-icons/go";

<Layout>
# شروع به کار با پلتفرم Flask
<hr className="mb-2" />

<a href="https://flask.palletsprojects.com/en/3.0.x/" className="blue-link">Flask</a> یک میکرو فریم‌ورک وب برای زبان برنامه‌نویسی پایتون است که به سادگی و انعطاف‌پذیری مشهور است. Flask به دلیل طراحی مینیمال خود، تنها ابزارهای ضروری برای توسعه وب را ارائه می‌دهد و این امکان را به توسعه‌دهندگان می‌دهد که با استفاده از افزونه‌ها و کتابخانه‌های مختلف، پروژه‌های خود را به راحتی گسترش دهند.

<div className="h-2" />
فریم‌ورک Flask بر پایه WSGI یا Web Server Gateway Interface ساخته شده و برای پروژه‌های کوچک تا متوسط مناسب است. Flask همچنین از سرور توسعه داخلی، مسیریابی URL و قالب‌های Jinja2 پشتیبانی می‌کند.
شما می‌توانید در سریع‌ترین زمان ممکن و به سادگی چند کلیک یا اجرای چند دستور ساده، برنامه خود را در لیارا مستقر کنید. در ادامه بخوانید:

<br />

<div className="grid grid-cols-2 gap-4">
            {[
    {
      text: 'استقرار سریع یک برنامه Flask در لیارا',
      link: './quick-start',
    },
    {
      text: 'استقرار قدم به قدم برنامه‌های Flask در لیارا',
      link: './how-tos/create-app',
    },
  ].map(item =>
              <Link href={item.link}>
                <Card className="flex cursor-pointer w-full items-center justify-between">
                  <div className="flex items-center gap-3">
                    <h4>
                      {item.text}
                    </h4>
                  </div>
                  <GoArrowLeft className="ml-1" />
                </Card>
              </Link>
            )}
          </div>

<br />

<Alert variant="success">
همچنین بخوانید:
<Link href='./related-links' className="blue-link">
 لینک‌های مرتبط با پلتفرم Flask
</Link>
</Alert>
<br />

در ادامه، ده تا از پربازدیدترین مستندات مربوط به Flask برای شما قرار گرفته است:

<ul className="pr-4">
  {[
    {
      text: 'انتخاب نسخه NodeJS',
      href: './',
    },
    {
      text: 'استقرار برنامه با Prisma',
      href: './how-tos/create-app',
    },
    {
      text: 'استقرار برنامه‌های Remix',
      href: '/',
    },
    {
      text: 'خطای CORS',
      href: '/',
    },
  ].map((item, index) => (
    <li
      key={index}
      style={{
        listStyle: "persian",
        textDecoration: "underline",
        textDecorationColor: "#9ca3af"
      }}
    >
      <a
        className="flex w-[max-content] items-center gap-2 text-[18px] mt-4"
        href={item.href}
      >
        {item.text}
        <GoArrowLeft className="ml-2 text-[15px] text-[gray]" />
      </a>
    </li>
  ))}
</ul>

</Layout>