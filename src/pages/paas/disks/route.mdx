import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import PlatformIcon from "@/components/Common/icons";
import {
    GoArrowLeft,
} from "react-icons/go";

import Head from "next/head";

<Layout>
<Head>
<title>مستندات تعریف مسیر برای دیسک‌ - لیارا</title>

<meta property="og:title" content="مستندات خدمات رایانش ابری لیارا" />
<meta property="og:description" content="مستندات مربوط به نحوه تعریف مسیر برای یک دیسک در لیارا"  />
<meta property="og:image" content="https://files.liara.ir/liara/logos/liara-poster.jpg" />
</Head>
# تعریف مسیر برای دیسک

<hr className="mb-2" />
       
<Tabs 
    tabs={["Liara Console", "Liara CLI"]} 
    content={[
    <>
    <p>
        برای اتصال دیسک به برنامه مدنظرتان، کافیست تا در مرحله دوم استقرار، نام دیسک را انتخاب کنید و مسیر مورد نظرتان را وارد کنید:
    </p>
    <div className="h-4" />
    <img src="https://files.liara.ir/liara/docs/step-2-mount-disk.png" alt="mount disk"/>
    <div className="h-4" />
    <p>
    مسیر دایرکتوری مدنظرتان را می‌توانید هم به صورت نسبی و هم به صورت absolute وارد کنید. به عنوان مثال، در عکس زیر دو دیسک به دو مسیر متفاوت، اولی نسبی و دومی به صورت کامل، متصل است:
    </p>
    <div className="h-4" />
    <img src="https://files.liara.ir/liara/docs/two-disks-console.png" alt="two disks mount"/>
    <div className="h-4" />
    <p>
    در نهایت، پس از استقرار کامل، دیسک مدنظرتان به حالت <b>در حال استفاده</b> در خواهد آمد:
    </p>
    <div className="h-4" />
    <img src="https://files.liara.ir/liara/docs/a-using-disk.png" alt="a using disk"/>
    <div className="h-4" />
    </>,
    
    <>
    <p>
    برای اتصال دیسک به دایرکتوری مدنظرتان، کافیست تا در مسیر اصلی پروژه، یک فایل به نام <Important>liara.json</Important> ایجاد کنید.  درون این فایل، باید یک آرایه به نام <Important>disks</Important> ایجاد کنید:
    </p>
    <div className='h-2' />
    <div dir="ltr">
    <Highlight className="json">
        {`{
    "disks": []
}`}
    </Highlight>
    </div>
    <div className='h-2' />
    <p>
    درون این آرایه، می‌توانید دیسک‌های مدنظرتان را به دایرکتوری‌های مختلف موجود در پروژه، متصل کنید. کافیست برای هر اتصال، یک object جدید باز کنید و در فیلد <Important>name</Important> باید شناسه دیسک  و در فیلد <Important>mountTo</Important> باید مسیری را وارد کنید که قصد دارید دیسک به آن متصل شود؛ به عنوان مثال:
    </p>
    <div className='h-2' />
    <div dir="ltr">
    <Highlight className="json">
        {`{
    "disks": [
        {
            "name": "media",
            "mountTo": "uploads/media"
        }
    ]
}`}
    </Highlight>
    </div>
    <div className='h-2' />
    <p>
    مسیر دایرکتوری مدنظرتان را می‌توانید هم به صورت نسبی و هم به صورت absolute وارد کنید. به عنوان مثال، در قطعه کد زیر دو دیسک به دو مسیر متفاوت، اولی نسبی و دومی به صورت کامل، متصل است:
    </p>
    <div className='h-2' />
    <div dir="ltr">
    <Highlight className="json">
        {`    "disks": [
        {
            "name": "mydisk1",
            "mountTo": "/app/uploads/media"
        },
        {
            "name": "mydisk2",
            "mountTo": "myfiles/dirs"
        }
    ]
}`}
    </Highlight>
    </div>
    <div className='h-2' />
    <p>
    در نهایت، شما می‌توانید با اجرای دستور <Important>liara deploy</Important> برنامه خود را در لیارا مستقر کنید؛ با این کار، دیسک مدنظرتان به حالت <b>در حال استفاده</b> در خواهد آمد:
    </p>
    <div className="h-4" />
    <img src="https://files.liara.ir/liara/docs/a-using-disk.png" alt="a using disk"/>
    <div className="h-4" />
    </>,

    ]} 
/>
<Section id="see-also" title="همچنین بخوانید:" />
<div className="grid md:grid-cols-2 gap-4">
            {[
    { alt: 'nodejs', platform: 'NodeJS', link: "/paas/nodejs/how-tos/use-disk" },
    { alt: 'next', platform: 'NextJS', link: "/paas/nextjs/how-tos/use-disk" },
    { alt: 'laravel', platform: 'Laravel', link: "/paas/laravel/how-tos/use-disk" },
    { alt: 'php', platform: 'PHP', link: "/paas/php/how-tos/use-disk" },
    { alt: 'dotnet', platform: 'NET.', link: "/paas/dotnet/how-tos/use-disk" },
    { alt: 'flask', platform: 'Flask', link: "/paas/flask/how-tos/use-disk" },
    { alt: 'django', platform: 'Django', link: "/paas/django/how-tos/use-disk" },
    { alt: 'docker', platform: 'مبتنی بر Docker', link: "/paas/docker/how-tos/use-disk" },
    
  ].map(item =>
              <Link href={item.link}>
                <Card className="flex cursor-pointer w-full items-center justify-between">
                  <div className="flex items-center gap-3">
                    <div className="w-[40px] p-1  bg-[#333] rounded-lg">
                      <PlatformIcon platform={item.alt} />
                    </div>
                    <h4>
                      اتصال دیسک به برنامه  {item.platform}
                    </h4>
                  </div>
                  <GoArrowLeft className="ml-1" />
                </Card>
              </Link>
            )}
          </div>

</Layout>