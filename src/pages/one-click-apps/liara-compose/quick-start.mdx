import Layout from "@/components/Layout";
import Button from "@/components/Common/button";
import Section from "@/components/Common/section";
import Alert from "@/components/Common/alert";
import Tabs from "@/components/Common/tab";
import Step from "@/components/Common/step";
import Card from "@/components/Common/card";
import Important from "@/components/Common/important";
import Highlight from "@/components/Common/highlight";
import Link from "next/link";
import { GoArrowLeft } from "react-icons/go";
import Head from "next/head";

<Layout>
  <Head>
    <meta property="og:title" content="مستندات خدمات رایانش ابری لیارا" />
    <meta property="og:description" content="مستندات مربوط به راه‌اندازی سریع برنامه با Liara Compose و آشنایی با تمام فیلد‌های آن" />
    <meta property="og:image" content="https://media.liara.ir/logos/liara-poster.jpg" />
    <title>مستندات راه‌اندازی سریع با Liara Compose - لیارا</title>
  </Head>

  # راه‌اندازی سریع برنامه با Liara Compose
  <hr className="mb-2" />
  <div className="h-2" />

  <video
    src="https://media.liara.ir/compose/liara-compose.mp4"
    controls
    className="block w-full"
    width="100%"
  />
  <br />

  برای راه‌اندازی برنامه‌های خود با استفاده از **Liara Compose** کافیست تا گام‌های زیر را طی کنید:

  <Step steps={[
    {
      step: "۱",
      content: (
        <>
          <h3>ورود به حساب کاربری</h3>
          <p>
            ابتدا وارد <Link className="text-[#2196f3]" href="https://console.liara.ir">کنسول لیارا</Link> شوید و با ایمیل یا شماره‌همراه خود وارد حساب کاربری‌تان شوید.
          </p>
        </>
      )
    },
    {
      step: "۲",
      content: (
        <>
          <h3>ایجاد برنامه جدید از نوع Liara Compose</h3>
          <p>
            پس از ورود به پنل، به بخش <strong>برنامه‌های آماده</strong> بروید و روی گزینه <Link className="text-[#2196f3]" href="https://console.liara.ir/one-click-apps/custom">Liara Compose</Link> کلیک کنید.
            در صفحه جدید، یک ویرایشگر YAML مشاهده می‌کنید که می‌توانید فایل Compose خود را وارد یا ویرایش کنید.
          </p>
        </>
      )
    },
    {
      step: "۳",
      content: (
        <>
          <h3>نوشتن فایل Liara Compose</h3>
          <p>
            فایل <code>liara-compose.yml</code> ساختار مشابه Docker Compose دارد و شامل تعریف سرویس‌ها، ایمیج‌ها، پورت‌ها و متغیرهای محیطی است. یک نمونه ساده:
          </p>
          <Highlight language="yaml">{`
services:
  app:
    image: node:18
    ports:
      - 3000:3000
    env:
      - NODE_ENV=production
    command: ["npm", "start"]
`}</Highlight>
        </>
      )
    },
    {
      step: "۴",
      content: (
        <>
          <h3>راه‌اندازی برنامه</h3>
          <p>
            پس از تنظیم فایل YAML، روی دکمه <strong>راه‌اندازی</strong> کلیک کنید. لیارا تمام سرویس‌ها را طبق فایل وارد شده بالا می‌آورد.
          </p>
          <Important>
            پس از راه‌اندازی، می‌توانید لاگ‌ها و وضعیت برنامه را از طریق کنسول لیارا بررسی کنید.
          </Important>
        </>
      )
    }
  ]} />

  <Section>
    <h2>نکات تکمیلی</h2>
    <ul className="list-disc pr-6 mt-4 text-right">
      <li>می‌توانید از چند سرویس در یک فایل استفاده کنید (مثلاً دیتابیس + اپلیکیشن).</li>
      <li>برای استفاده از پایگاه‌های داده، بهتر است از سرویس‌های دیتابیس لیارا نیز استفاده کنید.</li>
      <li>محدودیتی در استفاده از ایمیج‌های DockerHub وجود ندارد.</li>
    </ul>
  </Section>

  <Alert>
    اگر با ساختار فایل <code>liara-compose.yml</code> آشنایی ندارید، به <Link className="text-[#2196f3]" href="/one-click-apps/liara-compose/reference">مستندات مرجع Liara Compose</Link> مراجعه کنید.
  </Alert>

  <div className="mt-8">
    <Link href="/one-click-apps" className="inline-flex items-center text-[#2196f3] hover:underline">
      <GoArrowLeft className="ml-1" />
      بازگشت به لیست برنامه‌های آماده
    </Link>
  </div>
</Layout>
