Original link: https://docs.liara.ir/references/cli/list-dbs/

# مشاهده لیستی از دیتابیس‌های ایجاد شده با Liara CLI

برای  
مشاهده لیستی از دیتابیس‌های ایجاد شده با Liara CLI،  
می‌توانید از دستور زیر، استفاده کنید:  

```bash
liara db list # or liara db ls
```

## پارامترهای دستور
دستور `liara db list`یا `liara db ls`، پارامترهای زیر را، می‌پذیرد:

```bash
liara db ls [-h] [--debug] [--api-token <value>]  [--account <value>] [--columns
<value> | -x] [--filter <value>] [--no-header | [--csv | --no-truncate]] [--output csv|json|yaml] [--sort
<value>] [--team-id <value>]
```

## all links

[All links of docs](https://docs.liara.ir/all-links-llms.txt)
