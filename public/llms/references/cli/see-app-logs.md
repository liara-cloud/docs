Original link: https://docs.liara.ir/references/cli/see-app-logs/

# مشاهده لاگ‌های برنامه با Liara CLI

برای  
مشاهده لاگ‌های برنامه با Liara CLI،  
می‌توانید از دستور زیر، استفاده کنید:  

```bash
liara logs
```

## پارامترهای دستور
دستور `liara app:logs`یا `liara logs`، پارامترهای زیر را، می‌پذیرد:

```bash
liara logs [-h] [--debug] [--api-token <value>]  [--account <value>] [-a <value>]
[-s <value>] [-t] [-f] [-c] [--team-id <value>] [-r <version>] [--release <version>]
```

## all links

[All links of docs](https://docs.liara.ir/all-links-llms.txt)
