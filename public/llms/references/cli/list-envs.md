Original link: https://docs.liara.ir/references/cli/list-envs/

# لیست متغیرهای محیطی برنامه با Liara CLI

برای  
لیست متغیرهای محیطی برنامه با Liara CLI،  
می‌توانید از دستور زیر، استفاده کنید:  

<div dir="ltr">
```
liara env:list
```
</div>

## پارامترهای دستور
دستور `liara env:list`یا `liara env:ls`، پارامترهای زیر را، می‌پذیرد:  

<div dir="ltr">
```
liara env list [-h] [--debug] [--api-token <value>]  [--account <value>] [-a
<value>] [--columns <value> | -x] [--filter <value>] [--no-header | [--csv | --no-truncate]] [--output csv|json|yaml
|  | ] [--sort <value>] [--team-id <value>]
```
</div>

## all links

[All links of docs](https://docs.liara.ir/all-links-llms.txt)
