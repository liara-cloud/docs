Original link: https://docs.liara.ir/references/cli/list-buckets/

# مشاهده لیستی از باکت‌های ایجاد شده با Liara CLI

برای  
مشاهده لیستی از باکت‌های ایجاد شده با Liara CLI،  
می‌توانید از دستور زیر، استفاده کنید:  

```bash
liara bucket ls # or liara bucket list
```

## پارامترهای دستور
دستور `liara bucket ls`یا `liara bucket list`، پارامترهای زیر را، می‌پذیرد:

```bash
liara bucket ls [-h] [--debug] [--api-token <value>]  [--account <value>] [--columns
<value> | -x] [--filter <value>] [--no-header | [--csv | --no-truncate]] [--output csv|json|yaml] [--sort
<value>] [--team-id <value>]
```

## all links

[All links of docs](https://docs.liara.ir/all-links-llms.txt)
